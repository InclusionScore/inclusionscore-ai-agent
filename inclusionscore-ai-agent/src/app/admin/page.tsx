import { AppShell } from "@/components/app-shell";

export default function AdminPage() {
  return (
    <AppShell>
      <section className="card">
        <h1>Admin</h1>
        <p className="muted">Module placeholder for tenants, users, standards, workflows, and audit logs.</p>
      </section>
    </AppShell>
  );
}

