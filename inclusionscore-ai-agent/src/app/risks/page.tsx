import { AppShell } from "@/components/app-shell";

export default function RisksPage() {
  return (
    <AppShell>
      <section className="card">
        <h1>Risks</h1>
        <p className="muted">Module placeholder for workforce risk indicators, registers, and treatment plans.</p>
      </section>
    </AppShell>
  );
}

