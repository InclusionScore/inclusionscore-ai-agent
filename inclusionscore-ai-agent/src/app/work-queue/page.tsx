import { AppShell } from "@/components/app-shell";

export default function WorkQueuePage() {
  return (
    <AppShell>
      <section className="card">
        <h1>Work Queue</h1>
        <p className="muted">Module placeholder for tasks, approvals, service requests, and evidence reviews.</p>
      </section>
    </AppShell>
  );
}

