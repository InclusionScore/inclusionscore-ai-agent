import { AppShell } from "@/components/app-shell";

export default function ControlsPage() {
  return (
    <AppShell>
      <section className="card">
        <h1>Controls</h1>
        <p className="muted">Module placeholder for common controls and standards crosswalk mappings.</p>
      </section>
    </AppShell>
  );
}

